spring.application.name=backend
# --- CONEXIÓN A AZURE SQL ---
# Reemplaza 'sql-subastas-app' con el nombre de tu servidor en Azure
spring.datasource.url=jdbc:sqlserver://sql-subastas-app.database.windows.net:1433;database=db_subastas_core;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;

# Tu usuario y contraseña de Azure
spring.datasource.username=admin_subastas
spring.datasource.password=Paras1t0S.

# --- CONFIGURACIÓN JPA (Hibernate) ---
# Usamos 'none' o 'validate' porque YA creamos las tablas con el script SQL
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# --- PUERTO DEL SERVIDOR ---
server.port=8080

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# --- AZURE BLOB STORAGE ---
azure.storage.connection-string=${AZURE_STORAGE_KEY}
azure.storage.container-name=productos-img
